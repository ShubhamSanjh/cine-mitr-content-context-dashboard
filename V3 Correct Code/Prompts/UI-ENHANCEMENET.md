we have to implement forllowign changes for the next UI 


as we will start implemnt the import functionlity then on UI it should show how many records are imported and how many are failed to import with the reason for failure.
after import we will show one pop up with the details of the import and then we will show the imported records in the UI with the status of each record whether it is imported successfully or failed to import.
also we will add a filter option to filter the records based on the status of import (imported successfully or failed to import) and also we will add a search option to search for specific records based on the name or any other field.
we will also add a feature to export the imported records to a CSV file for further analysis and reporting. 
the export option will allow the user to select the fields they want to export and also the format of the exported file (CSV, Excel, etc). 
we will also add a feature to schedule the import process to run at specific intervals (daily, weekly, monthly) and also to send notifications to the user when the import process is completed with the details of the import (number of records imported successfully, number of records failed to import, etc). 
this will help the user to keep track of the import process and also to take necessary actions based on the results of the import. 
overall, these enhancements will improve the user experience and make the import process more efficient and effective for the users.


Error records should be highlighted in red color and the reason for failure should be displayed in a tooltip when the user hovers over the error record.
The filter option should allow the user to select multiple status options (imported successfully, failed to import) to view the records accordingly.
The search option should provide real-time search results as the user types in the search query.
The export feature should allow the user to select the desired fields for export and also provide options for the format of the exported file (CSV, Excel, etc).
The scheduling feature should allow the user to set specific intervals for the import process (daily, weekly, monthly) and also provide options for sending notifications (email, in-app notifications, etc) when the import process is completed with the details of the import (number of records imported successfully, number of records failed to import, etc).
The UI should also provide a summary of the import process, including the total number of records imported successfully, the total number of records that failed to import, and the overall success rate of the import process. This summary should be displayed prominently on the UI to give users a quick overview of the import process and its results. Additionally, the UI should allow users to easily access and review the details of each imported record, including any error messages or reasons for failure, to facilitate troubleshooting and improve the overall user experience.