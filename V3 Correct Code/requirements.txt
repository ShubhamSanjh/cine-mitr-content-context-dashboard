# ---------- Core ----------
fastapi==0.115.*
uvicorn[standard]==0.34.*
pydantic>=2.4.0,<3.0          # CVE-2024-3772 fixed in 2.4.0+
pydantic-settings>=2.0,<3.0
gunicorn>=22.0,<23.0; sys_platform != "win32"  # Production process manager (Linux/Mac only)

# ---------- Database ----------
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0     # PostgreSQL driver (swap for asyncpg if using async)

# ---------- Export / Reports ----------
openpyxl>=3.1,<4.0             # Excel export/import support
python-multipart>=0.0.28       # File upload support for import

# ---------- Dev / Testing ----------
httpx>=0.28,<1.0               # For TestClient
pytest>=8.0,<10.0
pytest-asyncio>=0.25,<1.0
